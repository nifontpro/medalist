import type {NextPage} from 'next';
import {useEffect} from "react";

const Home: NextPage = () => {

     useEffect(() => {

    }, [])

    return (
        <div>

        </div>
    );
};

export default Home;
