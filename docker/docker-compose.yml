version: '3.9'

services:
  nginx:
    image: nginx
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
      - ./nginx/localhost.cer:/root/ssl/localhost.cer
      - ./nginx/localhost.pem:/root/ssl/localhost.pem

